import java.util.Arrays;

public class SortExample {
    public static void main(String[] args) {
        int[] numbers = {5, 2, 8, 1, 3};

        // Built-in sort
        Arrays.sort(numbers);

        System.out.println("Sorted array (built-in): " + Arrays.toString(numbers));
    }
}
